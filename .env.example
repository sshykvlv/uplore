# ─── Database ────────────────────────────────────────────────────────────────
# Path to the SQLite database file. Directory is created automatically.
# In Docker the default is /data/uplore.db (set in the image).
DATABASE_PATH=./data/uplore.db

# ─── Uploads ─────────────────────────────────────────────────────────────────
# Directory where uploaded images are stored.
# In Docker the default is /data/uploads (set in the image).
UPLOADS_PATH=./uploads

# ─── Session ─────────────────────────────────────────────────────────────────
# Random secret used to sign session cookies (HMAC-SHA256).
# Generate with: openssl rand -base64 32
SESSION_SECRET=replace-me-with-a-random-secret

# ─── Telegram auth ───────────────────────────────────────────────────────────
# Bot token from @BotFather. Used server-side to verify the widget callback.
# Set the domain in BotFather with: /setdomain -> your-domain.example.com
TELEGRAM_BOT_TOKEN=

# Bot @username (without the @). Used to render the Login Widget button.
# This var is read server-side at runtime by the login page server component,
# so no rebuild is required when you change it (unlike truly NEXT_PUBLIC_ vars
# that are embedded at build time in client bundles). However, it is named
# NEXT_PUBLIC_ so that it remains available to client components if needed in
# the future — in all current uses it is read on the server.
# Must match the bot whose TELEGRAM_BOT_TOKEN you set above.
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=

# ─── Dev login ───────────────────────────────────────────────────────────────
# Set to 'true' to enable /api/auth/dev (fake username login).
# NEVER enable in production (also blocked when NODE_ENV=production).
ALLOW_DEV_LOGIN=true

# ─── Site ────────────────────────────────────────────────────────────────────
# Public URL of the deployment (used for redirects and OG tags).
PUBLIC_URL=http://localhost:3000

# ─── Team access gate (optional) ─────────────────────────────────────────────
# Leave both unset (or empty) to keep the site fully public — safe default for
# open-source self-hosters. When ACCESS_CODE is set every route is protected by
# a password gate until the visitor enters the code or visits the shareable link.
#
# ACCESS_CODE — the shared team password.
#   Setting this enables the gate. Anyone who knows it (or has the link below)
#   gets a 180-day cookie and is not asked again.
ACCESS_CODE=

# GATE_TOKEN — the opaque value stored in the uplore_gate cookie.
#   Optional. If unset, the token is auto-derived as: granted_<ACCESS_CODE>
#   You only need to set this explicitly if you want to rotate the cookie
#   independently of the password (e.g. to invalidate all existing sessions
#   without changing the password people type).
#   Must be set to the same value in both middleware and the /api/gate route;
#   since both use the same formula they will always agree when this is unset.
GATE_TOKEN=

# Shareable link format (no typing required):
#   https://<your-host>/gate?code=<ACCESS_CODE>
#   Visiting this URL auto-submits the code and sets the cookie immediately.

# ─── Analytics (optional) ────────────────────────────────────────────────────
# Umami self-hosted analytics. Leave blank to disable entirely.
# NEXT_PUBLIC_* vars are embedded at build time — rebuild after changing these.
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
NEXT_PUBLIC_UMAMI_URL=
